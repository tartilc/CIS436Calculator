<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/calcDisplayFragment"
            android:name="com.example.calculator.DisplayFragment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:foregroundGravity="center"
            tools:layout="@layout/display" />

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/buttonPadFragment"
            android:name="com.example.calculator.ButtonFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:layout="@layout/button_pad" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>